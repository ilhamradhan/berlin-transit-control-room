# TransitOps Berlin agent rules

- Before planning, writing, refactoring, debugging, or reviewing code, load `ponytail:ponytail` at `full`.
- Follow the loaded skill instead of copying its rules into project files.
- Keep it active for every coding response until the user says `stop ponytail` or `normal mode`.
- Do not simplify away trust-boundary validation, data-loss prevention, security, accessibility, explicit requirements, or the smallest runnable check for non-trivial logic.
- Do not commit or push unless the user explicitly asks.
